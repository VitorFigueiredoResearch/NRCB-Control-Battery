# Future Wrappers

Generic user-supplied data wrappers are future implementation items and are not runnable in v1.0.

Planned wrappers may include user CSV validation, example smoke tests, and user radial-profile diagnostics. No arbitrary-data support is claimed in this release candidate.